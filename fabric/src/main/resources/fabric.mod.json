{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",

  "name": "${name}",
  "description": "${description}",
  "authors": ["${author}"],
  "contact": {},

  "license": "${license}",
  "icon": "icon.png",

  "environment": "*",
  "entrypoints": {
    "main": [
      "${fabric_main}"
    ]
  },
  "mixins": [
    "rsec.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "minecraft": "=${minecraft_version}",
    "architectury": ">=${architectury_version}",
    "restapi": ">=${rest_api_version}-${minecraft_version}-fabric"
  }
}
